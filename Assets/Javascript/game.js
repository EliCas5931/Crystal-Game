// Create the variables needed for this...
var targetNumber;
var winLoss;
var crystals;
var score;
var userScore;

// Target number the user is trying to get to...
// Create a function to generate a random number for user to guess
for (var i = 0; i < 4; i++) {

var randomNumber = Math.floor(Math.random() * 12);
console.log(randomNumber);

var crystals = $("<div>");
    crystals.attr("class", 'crystals');

    $(".crystals").append(crystal);

}

// 4 crystals will generate random numbers 1-12

// Random new number should generate every win/loss

//